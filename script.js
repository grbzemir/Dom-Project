
// let veri;

// veri = window.document;
// veri = window.document.head;
// veri = window.document.body;
// veri = window.document.URL;
// veri = window.document.domain;
// veri = window.document.title;
// veri = window.document.images

// veri = window.document.forms[0].id; // id alındı
// veri = window.document.forms[0].method; // method alındı
// veri = window.document.forms[0].action; // action alındı
// veri = window.document.scripts; // script etiketleri alındı
// veri = window.document.scripts[2].getAttribute('src'); // src alındı


// console.log(veri); // Çıktı: <body>...</body>